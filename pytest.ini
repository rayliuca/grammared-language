[pytest]
# Test discovery patterns
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Test paths
testpaths = tests

# Minimum version
minversion = 7.0

# Output options
addopts = 
    -ra
    --strict-markers
    --strict-config
    --showlocals
    -v

# Markers
markers =
    unit: Unit tests that don't require external services
    integration: Integration tests that require services (gRPC, API)
    functional: Functional tests for Triton models
    slow: Tests that take a long time to run
    requires_triton: Tests that require Triton server
    requires_grpc: Tests that require gRPC server

# Coverage options (when using pytest-cov)
# Uncomment after installing pytest-cov
# [coverage:run]
# source = grammared_language
# omit = 
#     */tests/*
#     */test_*.py

# [coverage:report]
# exclude_lines =
#     pragma: no cover
#     def __repr__
#     raise AssertionError
#     raise NotImplementedError
#     if __name__ == .__main__.:
#     if TYPE_CHECKING:
